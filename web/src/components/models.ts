export interface Todo {
    id: number;
    content: string;
}

export interface Route {
    title: string;
    caption: string;
    icon: string;
    to: string;
}

export interface Meta {
    totalCount: number;
}
